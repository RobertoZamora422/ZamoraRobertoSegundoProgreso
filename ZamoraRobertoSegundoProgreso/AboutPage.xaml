<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZamoraRobertoSegundoProgreso.AboutPage"
             Title="Acerca de mi">

    <Grid RowDefinitions="*,*" ColumnDefinitions="*,*" Padding="20" BackgroundColor="White">
        <!-- Fila 1 - Columna 1 -->
        <Frame Grid.Row="0" Grid.Column="0" 
               CornerRadius="10" 
               BackgroundColor="#F5F5F5" 
               BorderColor="LightGray"
               Padding="15">
            <Label Text="Nombre: Roberto Zamora"
                   FontSize="18"
                   TextColor="Black"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Frame>

        <!-- Fila 1 - Columna 2 -->
        <Frame Grid.Row="0" Grid.Column="1" 
               CornerRadius="10" 
               BackgroundColor="#F5F5F5"
               BorderColor="LightGray"
               Padding="15">
            <Label Text="Fecha de nacimiento: 10/08/2005"
                   FontSize="18"
                   TextColor="Black"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Frame>

        <!-- Fila 2 - Columna 1 -->
        <Frame Grid.Row="1" Grid.Column="0" 
               CornerRadius="10" 
               BackgroundColor="#F5F5F5"
               BorderColor="LightGray"
               Padding="15">
            <Label Text="Intereses: Programacion, Musica y Cine"
                   FontSize="18"
                   TextColor="Black"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Frame>

        <!-- Fila 2 - Columna 2 -->
        <Frame Grid.Row="1" Grid.Column="1" 
               CornerRadius="10" 
               BackgroundColor="#F5F5F5"
               BorderColor="LightGray"
               Padding="15">
            <Label Text="Versión: 1.0"
                   FontSize="18"
                   TextColor="Black"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Frame>
    </Grid>
</ContentPage>